# Prompt V2 - Lane-Aware Harness

Migrate the GraphQL schema parser memory bank to permanent zero-padded IDs and
runner-compatible task tables while preserving completed M1-M5 history and
the metadata-only boundary.
